import { AuthClient } from "better-auth/client"

export default function SignupForm() {

    // const {data, error} = AuthClient.sign

  return (
    <div>
      <h1>Signup Form</h1>
    </div>
  )
}
